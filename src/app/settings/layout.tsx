export const metadata = {
  title: "Account settings",
  description: "Update your My Tutoring Hub name, phone, password, or delete your account.",
};

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
